__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.env

# NOTE: keywords.json, data/ and reports/ are intentionally committed —
# they are your public trend history, and the GitHub Actions workflow
# needs keywords.json + the SQLite history to compute trends.
